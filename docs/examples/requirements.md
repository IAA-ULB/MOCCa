# How to write testing scripts?

An acceptable testing script has the following properties:

1. It returns a simple exit code to indicate succes (0) or failure(1).
2. Self-contained
3. Allow for specification of the Tantalus executable on the command line
4. Allow for parameterization on the command line
5. is documented
6. and documents where the reference value(s) come from
